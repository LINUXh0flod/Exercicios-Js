let dispositivo = 'celula' 
switch (dispositivo) {
  case 'celula':
  console.log(`Voce esta usando um ${dispositivo}`)
break;
case 'tablet':
  console.log(`Voce esta usando um ${dispositivo}`)
  break;
  case 'computador':
  console.log(`Voce esta usando um ${dispositivo}`)
  break;


  default:
    console.log('Dispositivo invalido')
}

